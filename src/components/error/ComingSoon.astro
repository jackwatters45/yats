---
import { Button } from "@/components/ui/button";
import Layout from "@/layouts/Layout.astro";
---

<Layout>
  <div
    class="flex flex-col items-center justify-center space-y-4 py-16 text-center md:py-28 lg:py-36"
  >
    <div class="space-y-2">
      <h1 class="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl">
        Coming Soon
      </h1>
      <p
        class="max-w-[600px] text-stone-500 md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed dark:text-mate-muted-dark-400"
      >
        This page is currently under construction. We&apos;re adding the
        finishing touches to bring you something special. Coming very soon!
      </p>
    </div>
    <div class="space-x-4">
      <Button variant="outline">
        <a href="/">Go home</a>
      </Button>
      <Button variant="outline" id="go-back"> Go back </Button>
    </div>
  </div>
</Layout>
<script>
  function handleGoBack() {
    window.history.back();
  }

  document.getElementById('go-back')?.addEventListener('click', handleGoBack);
</script>
